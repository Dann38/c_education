#include "input.h"
int main(){    
    //input();
    test_input();
    //n = x1/h;
    //point (*points) = malloc(n * sizeof(point));
    //points = eiler(Tenv, k, x1, points);
    //print_kettle(points);
    //free(points);
    return 0;
}


